{
  "name": "seed",
  "version": "1.0.0",
  "description": "sample project",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ejs": "*",
    "express": "*",
    "mongodb": "*",
    "multer": "*"
  }
}
